import React from "react"

const colors = {
   textBlue: { color: "blue" },
   textRed: { color: "red" },
}

export default () => <div>
   <h1 style={ colors.textBlue }>Kissha</h1>
   <p style={ colors.textRed }>Shop your beauty</p>
</div>
